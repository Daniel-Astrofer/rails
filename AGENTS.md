# Repository scope

This repository owns network rail adapters that translate Kerosene service
requests to Bitcoin Core and LND. It must not own Auth, KFE, Vault, deployment
manifests or secrets. Preserve the existing HTTP contracts during extraction.
